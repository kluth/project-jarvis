module Quantum
complexity O(2^N)
{
    budget { power: 10000_nj }

    func collapse_state(input: Stream) -> Stream {
        prob {
            0.5 => return input,
            0.5 => return input * -1.0,
        }
    }
}
